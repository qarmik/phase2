# local backups + editor artifacts
*.bak
*.swp
*~

# generated report artifacts (keep repo small; track only curated evidence)
artifacts/reports/*.png
artifacts/reports/*.jsonld
artifacts/reports/heatmap_summary_*.json
artifacts/reports/risk_heatmap_*.png
artifacts/reports/remediation_plan_*.json
# NOTE: remove the next line if you WANT ownership files tracked for audit
artifacts/reports/ownership_*.json

# ensure local env / caches ignored
.venv/
__pycache__/
*.pyc
# Evidence & generated reports (do not commit)
artifacts/evidence/
artifacts/reports/
logs/
output/
input/
sample*.json
keys/*.pem
keys/archive/*.gpg
# local sensitive files and temporary downloads
downloaded_audit.zip
PrivatekeyIndusVai
keys/*.hmac.secret
keys/*.secret
keys/archive/*
